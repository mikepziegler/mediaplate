# ───── DATABASE CONFIG ─────
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=user
DB_PASSWORD=password
DB_NAME=mediaplate

# Full connection string (optional if using ORM config individually)
DATABASE_URL=postgres://user:password@localhost:5432/mediaplate

# ───── MINIO CONFIG ─────
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET=uploads

# ───── STORAGE ─────
STORAGE_DRIVER=minio
# Options: local | minio | s3

# ───── APP CONFIG ─────
PORT=3000
JWT_SECRET=dev-secret
JWT_EXPIRES_IN=1d